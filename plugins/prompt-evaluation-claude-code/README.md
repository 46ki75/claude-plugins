# prompt-evaluation-claude-code

A Claude Code plugin bundling the `prompt-evaluation-claude-code` Agent Skill:
eval-driven prompt refinement that runs entirely inside Claude Code via the
Agent/Task tooling.

## Install

```bash
/plugin marketplace add 46ki75/claude-plugins
/plugin install prompt-evaluation-claude-code@46ki75-plugins
```
