# kedb

A Claude Code plugin bundling the `kedb` Agent Skill: maintaining a Known Error
Database of documented errors, symptoms, workarounds, and root causes.

## Install

```bash
/plugin marketplace add 46ki75/claude-plugins
/plugin install kedb@46ki75-plugins
```
